{
  "name": "test",
  "description": "Runs all unit tests and reports the results.",
  "flags": [
    {
      "name": "env",
      "description": "Sets the build environment.",
      "type": "string"
    },
    {
      "name": "watch",
      "description": "Watches test files for changes and re-runs the tests automatically.",
      "type": "boolean"
    },
    {
      "name": "source",
      "description": "Sets the test to run off of the source files instead of the bundled and compiled files.",
      "type": "boolean"
    }
  ]
}
